build:
    image: latest

conda:
    file: .rtd-environment.yml

python:
    version: 3.6
    install:
        - requirements: docs/requirements.txt
        - method: pip
          path: .
    setup_py_install: true

formats:
    - pdf
